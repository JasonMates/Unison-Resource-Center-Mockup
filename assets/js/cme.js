/**
 * CME.html page interactions.
 * Shared navigation behavior lives in site-navigation.js.
 */
document.getElementById('cme-consultation-form').addEventListener('submit',event=>event.preventDefault());

